---
version: "2026-09-07"
name: "BookingNail Design System"
description: "หลังบ้านจองคิวร้านทำเล็บที่เบา อ่านเร็ว และไม่ทำให้เจ้าของร้านรู้สึกว่ากำลังใช้ระบบใหญ่เกินจำเป็น"
colors:
  surface: "#fffaf7"
  surfaceSoft: "#fff2eb"
  ink: "#3f2a35"
  muted: "#7b6670"
  border: "#efc9c4"
  primary: "#d85f7c"
  primaryDark: "#9f4058"
  accentCool: "#8dcfff"
  accentWarm: "#ffe8cd"
typography:
  display:
    fontFamily: "Prompt"
    fontWeight: 800
    lineHeight: 1.12
  body:
    fontFamily: "Prompt, Mali"
    fontSize: "16px"
    fontWeight: 500
    lineHeight: 1.55
  label:
    fontFamily: "Prompt"
    fontWeight: 800
    lineHeight: 1.25
layout:
  desktopContainer: "min(1120px, calc(100% - 28px))"
  mobileContainer: "min(100% - 14px, 460px)"
  desktopNav: "left rail"
  mobileNav: "bottom nav plus quick action menu"
rounded:
  control: "12px"
  panel: "18px"
  shell: "22px"
components:
  touchTargetMin: "44px"
  maxFrameDepth: 2
---

# BookingNail Design System

## Overview

BookingNail คือเครื่องมือจองคิวสำหรับร้านทำเล็บที่เจ้าของร้านต้องใช้ระหว่างทำงานจริง หน้าจอต้องอ่านเร็ว แตะง่าย และไม่รู้สึกว่าซับซ้อนเกินงานรับคิวประจำวัน

## Design Principle

ความเรียบง่ายสำคัญกว่าความตกแต่ง ทุกกรอบ สี เงา และปุ่มต้องช่วยให้เจ้าของร้านตอบคำถามนี้ได้เร็วขึ้น: วันนี้มีใครต้องทำอะไรต่อ

## Colors

- `primary` ใช้กับ action หลัก เช่น บันทึก ยืนยัน หรือแท็บที่เลือก
- `surface` และ `surfaceSoft` ใช้กับพื้นที่ทำงาน ไม่ควรแข่งกับข้อมูล
- `border` ใช้เป็นเส้นแบ่งเบา ๆ ไม่ใช้เพื่อสร้าง card ซ้อนหลายชั้น
- `accentCool` ใช้แต้มข้อมูลรองหรือธีมร้าน ไม่ควรเป็นสีหลักของหลังบ้าน
- สีสถานะต้องมีข้อความกำกับเสมอ ไม่สื่อด้วยสีอย่างเดียว

## Typography

- ใช้ `Prompt` เป็นฟอนต์หลักของระบบหลังบ้าน เพราะอ่านตัวเลข วันที่ และปุ่มได้ชัด
- ใช้หัวข้อใหญ่เฉพาะจุดเริ่มงาน เช่น ภาพรวมวันนี้
- ข้อความบนปุ่มต้องเป็นคำกริยาสั้น ๆ เช่น `บันทึกคิว`, `ยืนยัน`, `เสร็จแล้ว`, `ยกเลิกคิว`
- หลีกเลี่ยงคำระบบ เช่น configuration, provider, token หรือ backend ใน UI เจ้าของร้าน

## Layout

- Desktop ใช้ left rail สำหรับเมนู และพื้นที่หลักขวาสำหรับงาน
- Mobile ใช้ bottom nav และปุ่ม `+` สำหรับงานจัดการที่ไม่ต้องเปิดตลอด
- หน้า `ภาพรวม` เน้นงานวันนี้และคำขอที่ต้องตอบ
- หน้า `ตารางคิว` ต้องโล่งที่สุด เพราะใช้ scan วันที่และคิว
- หน้า `ลงคิว` ต้องเป็นฟอร์มตรงไปตรงมา ไม่มีข้อมูลอื่นรบกวน

## Frame Depth

กฎหลัก: ห้ามเกิน 2 ชั้นในพื้นที่เดียว

ดี:

```text
พื้นที่หลัก > รายการคิว
พื้นที่หลัก > ฟอร์มลงคิว
พื้นที่หลัก > ปฏิทิน + รายละเอียดวัน
```

หลีกเลี่ยง:

```text
พื้นที่หลัก > card > card > item card > pill หลายชุด
```

กรอบที่คงไว้ได้:

- รายการคิวแต่ละรายการ
- กลุ่มฟอร์มที่เป็นงานคนละชุด
- dialog/popup
- bottom navigation บน mobile

กรอบที่ควรลด:

- card ชั้นนอกที่ซ้ำกับพื้นที่หลัก
- shadow หลายระดับในหน้าเดียว
- border รอบ item ที่มี divider อยู่แล้ว

## Components

- Primary button ใช้กับ action สำคัญที่สุดหนึ่งจุดต่อกลุ่มงาน
- Secondary button ใช้กับทางเลือกที่ไม่ทำให้ข้อมูลเปลี่ยนทันที
- Danger button ใช้เฉพาะยกเลิก/ลบ และต้องมีข้อความชัด
- Status pill ใช้เฉพาะสถานะที่ต้อง scan เร็ว เช่น `รอยืนยัน`, `ยืนยันแล้ว`, `เสร็จแล้ว`
- Toast ใช้กับผลลัพธ์เล็ก ๆ
- Dialog ใช้กับผลลัพธ์หรือการยืนยันที่เจ้าของร้านควรเห็นชัด

## Mobile Rules

- ปุ่มหลักต้องแตะง่ายอย่างน้อย 44px
- หลีกเลี่ยง text ยาวใน bottom nav
- อย่าให้ floating/bottom UI บังปุ่มท้ายฟอร์ม
- ข้อมูลคิวควร scan ได้ใน 1-2 บรรทัดก่อนค่อยดูรายละเอียด
- ฟอร์มควรเป็นคอลัมน์เดียว

## Do's

- ใช้คำสั้น ตรงกับงานจริงของร้าน
- ให้ข้อมูลสำคัญมาก่อน: ชื่อ เวลา เบอร์ บริการ สถานะ
- ใช้เส้นแบ่งเบา ๆ แทน card ซ้อนเมื่อข้อมูลอยู่ในกลุ่มเดียวกัน
- ตรวจ mobile/iPad screenshot เมื่อแก้ UI
- bump `assetVersion` และ `CACHE_VERSION` เมื่อแก้ CSS/JS/PWA

## Don'ts

- ไม่เพิ่มกรอบเพื่อให้หน้าดูเต็ม
- ไม่ใช้ปุ่มหลายปุ่มที่น้ำหนักเท่ากันในงานเดียว
- ไม่แสดงข้อมูลลูกค้าในหน้าสาธารณะ
- ไม่ใช้คำเทคนิคกับเจ้าของร้าน
- ไม่ทำ mobile เป็น desktop ที่ย่อขนาดลงมาเฉย ๆ
